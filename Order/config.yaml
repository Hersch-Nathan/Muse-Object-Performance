show:
  name: "Assembled"
  runs_label: "Show Runs"
  start_time: "7:40 PM"
  run_count: 6
  step_minutes: 20
  intermission:
    every_n_runs: 4
    length_minutes: 20
    none_before_after: true  # If true, force None performers before and after intermission for performer breaks
  random_seed: null  # Set to a number for reproducible random permutation order, null for no randomization

characters:
  - name: "Domin"
    offset_start_min: 0
    offset_end_min: 10
  - name: "Alquist"
    offset_start_min: 8
    offset_end_min: 15

performers:
  - "Moose"
  - "Luca"

objects:
  - name: "Shirt"
    performers: ["all"]  # "all" or list of performer names
  - name: "Muppet"
    performers: ["all"]
  - name: "Robot"
    performers: ["all"]
